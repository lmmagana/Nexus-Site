/**
 * Autocomplete function declaration, do not delete
 * @param {import('./__schema__.js').Payload} options
 */
export const invoke = async ({payload}) => {
  // get contact
  // Check if member
  // if not, make member and send invitation for password
  return {}
};